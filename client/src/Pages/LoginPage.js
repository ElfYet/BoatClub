import React from 'react';
import Login from '../Components/Login'

const LoginPage = () => {
    return (
        <div>
            <Login />
            {/* Login page content */}
        </div>
    );
};

export default LoginPage;